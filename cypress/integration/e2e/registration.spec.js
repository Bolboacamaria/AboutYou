/// <reference types="cypress" />
import selectors from '../../support/selectors.js';

//TODO: possibly bulk registations, woth cases specified on test cases list .
describe('', () => {
    before(() => {

    })

    it('', () => {

    })

})
