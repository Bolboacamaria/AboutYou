/// <reference types="cypress" />
import selectors from '../../support/selectors.js';

//TODO: would use commands to add the login   "Cypress.Commands.add" in commands.js file. and access them from here 
describe('', () => {
    it('', () => {

    })

})
